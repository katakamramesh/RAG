# MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI}

# Server Configuration
server.port=8080

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# API Keys Configuration (comma-separated for multiple keys)
api.keys=${API_KEYS}

# Logging Configuration
logging.level.root=INFO
logging.level.com.example.ragchat=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.data.mongodb=INFO

# Validation Messages
spring.messages.basename=messages

#http://localhost:8081/db/ragchat/chat_sessions
#http://localhost:8080/swagger-ui/index.html#/chat-session-controller/createSession