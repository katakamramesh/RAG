# MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI}

# Server Configuration
server.port=8080

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# API Keys Configuration (comma-separated for multiple keys)
api.keys=${API_KEYS}

# LLM Configuration (Hugging Face)
llm.api.key=${LLM_API_KEY}
llm.api.url=${LLM_API_URL:https://api-inference.huggingface.co/models/mistralai/Mistral-7B-Instruct-v0.2}
llm.model=${LLM_MODEL:mistralai/Mistral-7B-Instruct-v0.2}
llm.max.tokens=${LLM_MAX_TOKENS:1000}
llm.temperature=${LLM_TEMPERATURE:0.7}

# Logging Configuration
logging.level.root=INFO
logging.level.com.example.ragchat=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.data.mongodb=INFO

# Validation Messages
spring.messages.basename=messages

spring.datasource.url=jdbc:postgresql://db:5432/ragdb
spring.datasource.username=raguser
spring.datasource.password=ragpass
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true



#http://localhost:8081/browser/
#http://localhost:8080/swagger-ui/index.html#/chat-session-controller/createSession